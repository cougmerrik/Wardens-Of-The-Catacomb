interface:
  display_name: "Blender MCP"
  short_description: "Create Blender assets interactively or headlessly"
  default_prompt: "Use $blender-mcp to create and validate a low-poly Blender asset through interactive MCP or safe headless Blender."

dependencies:
  tools:
    - type: "mcp"
      value: "blender"
      description: "Official Blender Lab MCP server"
      transport: "stdio"
